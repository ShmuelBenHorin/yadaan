import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder test', (WidgetTester tester) async {
    // טסט בסיסי — האפליקציה עוברת קומפילציה
    expect(true, isTrue);
  });
}
